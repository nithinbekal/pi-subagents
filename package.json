{
  "name": "pi-subagents",
  "version": "0.3.0",
  "description": "Tmux-based isolated Pi workers with durable report delivery and conservative lifecycle cleanup",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi-coding-agent",
    "subagents",
    "tmux"
  ],
  "files": [
    "extensions",
    "skills",
    "docs",
    "README.md",
    "LICENSE",
    "protocol.json"
  ],
  "bin": {
    "subagents": "skills/subagents/subagents"
  },
  "scripts": {
    "test": "bash -n skills/subagents/subagents tests/*.sh && node --check skills/subagents/state.mjs && node --experimental-strip-types --test tests/config.test.ts && bash tests/cli-smoke.sh && bash tests/cli-reliability.sh && bash tests/cli-lifecycle.sh && bash tests/public-safety.sh"
  },
  "pi": {
    "extensions": [
      "./extensions/subagents-watch.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "engines": {
    "node": ">=22.6"
  }
}
